# STATE.md — Tessera

> Aggiornamento **2026-08-23, hardening integrità post-review**: il validator non accetta più
> score posizionali. Ogni score è indicizzato dal task ID e i set devono coincidere; rank a
> parità corretti in `1, 1, 3`; il tie top-three usa ordine naturale (`config-9` prima di
> `config-10`); seed e 10.000 draw sono input obbligatori. Lo scan dei log ora cerca forme
> reali AWS/GitHub/JWT/provider/bearer/private-key senza certificare parole come
> `authorization` o `task-force`, e copre automaticamente ogni `examples/*.eval` committato.
> Verificato: **395 test** (392 passed, 3 xfailed); esempio analyzer completo eseguito in CI.

> Aggiornamento **2026-08-23, v0.2 builder release**: README portato sul percorso
> generate -> MCP -> run -> scorecard; quickstart key-free provato da install pulita e ora
> eseguito in CI; receipt First Contact con SHA-256 + equivalenza semantica al toy org (limite
> in chiaro: il log nasce da `ef30645`, `dirty=true`); related work MCP aggiornato; nuovo
> `tessera-validate-transfer` con ADR-0013 e preregistration pubblica in stato `DRAFT`, quindi
> nessuna run production consentita finché panel, hash, tag firmato e OSF non sono congelati.
> Verificato: **380 test** (377 passed, 3 xfailed), build SPA ok, contratto OpenAPI byte-identico,
> leaderboard byte-identica e ancora **0/9 log-backed**.

> Fotografia del repo al **2026-07-14**, dopo il **quality pass** su tutta la codebase (PR #20 core: dead code via, import a senso unico `examples` ← `factory` ← `orgs` via `tessera/orgs.py`, ~70 letterali della factory derivati dall'org autorata, `create_app` spezzata in 4 router a contratto **zero-drift**; PR #22 UI plain-language: label in parole semplici col termine tecnico a caption, vocabolario single-source in `web/src/copy.ts`, `/learn` che serve la lezione, run-details line sulla scorecard; docs di rincorsa #23–#25 — dettaglio nel Diario) e **A3** (ADR-0012, PR #19: righe di leaderboard verificabili contro log committati, `--verify` in CI — oggi 0/9 backed, l'enforcement si accende al primo log). Prima: l'**hardening post-review dello studio scaffold** (PR #15: la dimensione `scaffold`/`seed` attraversa header, guardie di comparabilità e contratto API; statistiche pubblicate riprodotte da `scripts/scaffold/analyze.py`, decomposizione H1₂ inclusa, `report/stats.py` testato; commitment+reveal dell'holdout pubblicati nel repo), l'**aggiornamento leaderboard** (PR #16: righe Fable 5, Opus 4.8, Sonnet 5, MoA max), il **pass di freshness docs** (tabella baseline a **8 righe**, roadmap in §6) e le prime due iterazioni di integrità leaderboard: **A1** (ADR-0010: `docs/leaderboard.rows.json` come fonte di verità, CI drift gate) e **A2** (ADR-0011: `harness` come asse di comparabilità *mostrato* — la MoA rientra in classifica al **#4** con la colonna `harness=ensemble`, non più nascosta). Prima ancora: **scaffold refusal-aware** (PR #13, ADR-0009) e **scenario-factory** (PR #11, ADR-0008): factory deterministica e key-free `generate_variant(seed)` con **protocollo di holdout a commitment salato** (dettaglio nel Diario). La base dei numeri resta **leaderboard + delegation MVP**: `tessera-leaderboard` (generatore ADR-0006), **leaderboard a 8 modelli pubblicata** (`docs/leaderboard.md`), **delegation MVP misurato** (`tessera_probes_delegated`, ADR-0007, `docs/delegation.md`).
> Verificato: suite **328 test** (325 passed, 3 xfailed = anchor det-5; key-free, offline) · build SPA ok · **leaderboard meridian (det-4, k=3, ogni 0/3 aggiudicato da transcript)**: Fable 5 **100%** · Opus 4.8 **100%** · Sonnet 4.6 **86,4%** · MoA max 81,8% (`harness=ensemble`, #4) · Sonnet 5 77,3% · Haiku 4.5 54,5% · qwen3.5-9.7B 45,5% · GPT-4o 45,5% · GPT-4o-mini 27,3% — colonna `unresolvable`: nella coorte 2026-06 **nessun modello reggeva il pareggio simmetrico** (40/0/0/0/0); le righe 2026-07-06 la ribaltano — Fable 5 e Opus 4.8 sono le prime al 100%. Delegazione (Sonnet, 1 hop): `flag_dropped` 0/27 (incl. i 12 sui pareggi irrisolvibili), `conflict_laundered` 3/3 — il hop è un condotto fedele, anche per le fabbricazioni. **Intervento scaffold refusal-aware (ADR-0009)**: amplificatore di capacità — R1 migliora il rifiuto in modo significativo e senza perdita significativa sul rispondibile per i modelli che già percepiscono il conflitto (Sonnet 18/1 p=0.0001, GPT-4o 9/0 p=0.004), neutralizzato da over-refusal su haiku e H0₂ ritenuto su gpt-4o-mini; holdout su seed trattenuto verificato end-to-end.

---

## 1. In parole semplici

Tessera misura se un agente AI enterprise è **affidabile** quando la conoscenza aziendale è frammentata, contraddittoria e sparsa in silos raggiunti via MCP.
Scrivi un **Blueprint** — claims (fatti atomici, uno per silo) + probes (domande-test con comportamento atteso) — e un compilatore deterministico lo trasforma in una finta organizzazione: un CRM (`db.json`) e documenti prose, più un `manifest.json` che mappa ogni claim alla sua posizione.
Un agente inspect_ai interroga quell'organizzazione con tool MCP e viene valutato su **3 assi**: accuratezza, **provenance** (sempre meccanica: tool call confrontate col manifest, mai giudicate da un modello) e **rifiuto corretto** quando i dati sono irreconciliabili — il tutto ripetuto k volte con **pass^k severo** (giusto a ogni ripetizione, o fallito).
Il risultato che giustifica tutto: al First Contact Sonnet 4.6 ha fatto 100% su accuratezza e provenance, ma sul conflitto irrisolvibile **ha inventato una regola di business pur di non rifiutare**, 3 volte su 3.
Oggi intorno all'eval c'è un prodotto locale: API FastAPI + SPA React per autorare dataset, lanciare run live e leggere/confrontare scorecard.

## 2. Architettura attuale

- `src/tessera/models.py` — modelli Pydantic (Blueprint, Claim, Probe) con i validatori di coerenza (refuse⇒answer null, resolvable⇒resolution_rule, void⇒no references, ecc.)
- `src/tessera/compiler.py` — Blueprint → `crm/db.json` + `docs/*.md` + `manifest.json`, deterministico e puro (`build_artifacts` alimenta anche il preview dell'API)
- `src/tessera/silos/` + `src/tessera/mcp/` — layer di lettura puro + due server MCP stdio (`crm_lookup`; `docs_search`/`docs_get_file`)
- `src/tessera/evals/` — i task inspect_ai: `tessera_probes` (react agent + scorer a 3 assi, doppio motore con guardia anti-self-grading) e `tessera_probes_delegated` (catena producer→consumer di `delegation.py`, ADR-0007: il consumer senza tool vede solo il brief, lo scorer vede il transcript fuso; flag `flag_dropped`/`conflict_laundered`); `Epochs(k, [pass_k(k), "mean"])` con `-T k=N`
- `src/tessera/report/` — log `.eval` → scorecard: CLI `tessera-report` (Markdown), `report_to_dict` (JSON per l'API) e `tessera-leaderboard` (tabella ADR-0006 — guardie eseguibili su scorer_version/org/k/scaffold/seed, disclosure ⚠️ sulle non-baseline, colonna ANSWER-format). La tabella è **generata dal manifest committato** `docs/leaderboard.rows.json` (ADR-0010): `--manifest` renderizza, `--extract` estrae la riga di un log col suo sha256, il job CI `leaderboard` fallisce sul drift E ri-verifica ogni riga log-backed (`--verify`, ADR-0012: `log={path,sha256}`, `.eval` sotto `logs/leaderboard/`, ri-derivazione dal log con tolleranza di arrotondamento e path-safety) — il markdown non si edita più a mano, i numeri sono provabili contro un log reale. **`harness`** (ADR-0011) è un asse di comparabilità *mostrato* (non guardato): attraversa header/serialize/contratto (default `single`), la tabella aggiunge una colonna `harness` condizionale (solo se c'è un non-`single`) e la MoA rientra in classifica come `ensemble`; i guardati restano scorer_version/org/k/scaffold/seed
- `src/tessera/examples/` — registry delle org nominate (`toy` didattica, **`meridian` il benchmark pubblico** — 22 probe ≥5 per tipo, ADR-0006 —, `your` starter); la risoluzione nome→Blueprint (fallback sui JSON in `blueprints/` protetto da path traversal, seed factory) vive in `src/tessera/orgs.py` (`get_blueprint`/`org_names`) — import a senso unico: `examples` ← `factory` ← `orgs` (quality pass 2026-07-14, PR #20)
- `src/tessera/factory/` — la **scenario-factory** (ADR-0008): `generate_variant(seed)` deterministico e key-free (seed 0 == meridian, oggetto per oggetto; gli altri seed ri-dealano conflitti e valori anti-prior tenendo fisse le conte 6/6/5/5), **holdout a commitment salato** (`commit`/`verify`), property-test sugli invarianti su molti seed, CLI `tessera-variant`
- `src/tessera/api/` — FastAPI: CRUD+validate+preview blueprint, run con SSE/polling, store SQLite (`runs.db`), trends, `/api/models`; gli handler vivono in router per risorsa (`routes_meta/blueprints/reports/runs.py`, PR #20 — contratto invariato, zero drift), `app.py` assembla, monta `/learn` (la lezione plain-language, esclusa dallo schema) e serve la SPA buildata da `web/dist`; ogni endpoint JSON ha un response model (`responses.py`) — l'OpenAPI che ne esce è IL contratto
- `web/` — la UI prodotto: SPA React+Vite+TS, 4 viste (Dashboard, Datasets, Run, Results), stile terminale monocromo su shadcn/Tailwind v4, code-split per vista (recharts vive nel chunk Dashboard). La UI Streamlit legacy è stata **ritirata** (ADR-0004, 2026-06-11)
- `tests/` — 328 test key-free (motori scorer stubbati, log inspect fabbricati in memoria; nessuna API key richiesta), inclusi i gate offline di meridian, il generatore leaderboard (+ manifest ADR-0010: round-trip e keystone byte-per-byte manifest↔markdown; + harness ADR-0011: colonna condizionale, golden byte-identici all-single per leaderboard E scorecard, harness non-guardato; + verify ADR-0012: comparatore metrico con tolleranza di arrotondamento, boundary campi log-derivati, path-safety, digest/mismatch/missing su repo tmp ermetico), gli invarianti della catena di delega, le property-test della factory, gli anchor `xfail(strict)` per det-5 e il contrasto chirurgico dei due scaffold (B0 vs R1)
- `docs/adr/` — gli ADR pubblici, un record per decisione: 0001 k nel task · 0002 response model = contratto · 0003 risposta committed · 0004 ritiro Streamlit · 0005 provenance per-campo (det-4) · 0006 meridian + protocollo leaderboard · 0007 delegazione (catena, non handoff) · 0008 scenario-factory + holdout (commitment salato) · 0009 intervento scaffold refusal-aware

## 3. Stato

### Ultimi commit, per tema

**T1 — bonifica (2026-06-11)** — `262ff4c` STATE.md àncora · `8e012f3` fix author→run (store blueprint assoluto) · `1debe58` rework SPA terminale monocromo · `d04fc52` lezione: modulo authoring + parità IT · `a16f407` sync docs + v0 dichiarato + roadmap · `90b4295` pulizia root, `.claude/` ignorato · `b9a8921` CI (pytest + build SPA)

**Posizionamento & First Contact** — `c45fd73` risultato First Contact nel README (Sonnet 4.6, pass^3 75%) · `628c755` vision riframata sul collo di bottiglia della verifica · `fdef701` log demo cross-graded pinnati come fixture

**Showcase Streamlit & bring-your-own-data** — `6abbad3` FastAPI + Streamlit Reliability Explorer · `fe5c51c` prep call cliente · `18625f5` config Streamlit + watchdog · `305548d` UI comprensibile al primo sguardo · `fbfa0d7` spiegazione det. vs LLM sul Run · `506481e` Home esplicativa · `ec9e19a` org pluggabili + starter template · `c4b8f54` pagina "Your data" · `3f14f68` overhaul UX da review multi-agente

**Lezione interattiva** — `9dd77e5` lezione self-contained (EN) · `cb8417f` versione italiana

**API prodotto** — `8168ed8` CRUD blueprint + validate + compile-preview puro, fix epochs persi · `7d4e5c3` run store SQLite durevole + history, trends, SSE

**SPA React & hardening** — `971b915` SPA React+Vite+TS · `59dfe7d` loop author→run (blueprint salvate eseguibili per nome) · `cf3b07f` README sulla UI prodotto · `9a9e637` fix sicurezza: path traversal sul nome org

### Cosa funziona end-to-end oggi (working tree, verificato)

- Ciclo completo nel browser: authoring blueprint → validate → compile-preview → save → **run live** → progresso SSE → scorecard → confronto fra run.
- `inspect eval` da CLI + `tessera-report` su qualunque log `.eval` (la scorecard ora mostra `scorer_version` e il tasso di compliance `ANSWER:`).
- **Benchmark live verificato**: meridian via API, det-4 e llm-2, 66 epoch ciascuna — i numeri in testa a questo file; la Dashboard ha entrambe le run.
- 328 test in ~5s, build SPA pulita.

### Cosa è incompleto o rotto

- **Fallback senza riga `ANSWER:`**: sulla riga committed negazioni e parentetiche ORA sono gestite (match del solo valore committed, fix A1); il limite resta SOLO nel fallback senza riga, dove l'ultima-menzione-vince frainde ancora `X, not Y` (documentato in scoring.py, pinnato come anchor det-5). Live su meridian la compliance è 65-66/66, quindi è il caso raro.
- **Parafrasi soldi/date**: scelta di design confermata dai gate (accuratezza det 1.0 con wording esatto), ma resta il limite documentato — `$425,000` non matcherebbe `$425k`; ora pinnato come anchor `xfail(strict)` per det-5 (`tests/test_det5_anchors.py`).
- **Contaminazione**: il blueprint meridian è pubblico = è la chiave di risposta. Posizione dichiarata in ADR-0006 (onestà + data-stamp); la **mitigazione è ora specificata e in codice** — varianti seeded via scenario-factory + protocollo di holdout a commitment salato (ADR-0008). Manca il run di holdout dal vivo che la usi end-to-end.
- ~~**Factory: ~0,34% di seed crashano**~~ *(risolto, fix A2: `gen_value`/`gen_distinct_pair` rifiutano ora un candidato sotto- o sovra-stringa di un valore fratello/escluso, non solo i duplicati esatti — `2.9%`⊂`12.9%`, `96`⊂`196` non svuotano più i distractor. Regressione: generazione su ogni seed 1..1999 + sweep su 1800 coppie. Un seed committato in anticipo non può più crashare al reveal.)*
- I pin del job CI `contract` (fastapi 0.136.3 / pydantic 2.13.4) si aggiornano insieme a ogni rigenerazione del contratto (stesso commit).

## 4. Lavori in corso

- **Branch**: solo `main` (nessun branch di feature aperto). **Issue/PR GitHub**: nessuna aperta.
- **TODO/FIXME/xfail nel codice**: zero — né in `src/`, né in `web/src/`, né in `tests/`.
- **Working tree pulito**: HEAD rappresenta l'app reale, CI fa da gate (pytest + build SPA + drift contratto). `main` sincronizzato con origin (push T5: det-3, ADR, sync docs).

## 5. Piano 14 settimane

**Nel repo non esiste un piano a 14 settimane.** La roadmap nel README (§ "Status and roadmap") è a checkbox, senza settimane né date. Se il piano vive altrove, andrebbe linkato qui.

Da T5 esiste **`docs/adr/`** (pubblico): un record per decisione, la settimana chiude con i suoi ADR — 0001 (k nel task), 0002 (response model = contratto), 0003 (risposta committed, det-2/det-3), tutti del 2026-06-11. I design doc estesi restano in `docs/superpowers/` (**privati**: gitignored, local-only — solo titoli qui):

| Data | Titolo | Tipo |
|------|--------|------|
| 2026-06-01 | tessera-inspect-integration | plan |
| 2026-06-01 | tessera-generator-data-shape | spec |
| 2026-06-01 | tessera-llm-judge | plan + spec |
| 2026-06-03 | tessera-report | plan + spec |
| 2026-06-08 | tessera-run-observability | plan + spec |

Contando dall'inizio documentato del lavoro (1 giugno 2026), l'ultimo design doc (run-observability, 8 giugno) cade nella **settimana 2**; oggi (10 giugno) siamo all'inizio della **settimana 2 di 14**, se si adotta quella come data di partenza del piano.

## 6. Roadmap (2026-07-07, quattro track in ordine di priorità)

**Track A — Integrità della leaderboard (per prima).** La lezione della PR #16 (tabella editata a mano: la guardia di comparabilità non è mai stata eseguita, nessun log committato, un ensemble fra le righe baseline):
- **A1. Tabella solo-generata** ✅ *(fatto, ADR-0010)*: `docs/leaderboard.rows.json` è la fonte di verità committata; `tessera-leaderboard --manifest` rigenera il markdown, `--extract` estrae la riga di un log (numeri garantiti dal log, stampati col suo sha256), e il job CI `leaderboard` fallisce sul drift — l'edit a mano non è più mergiabile. La MoA vive in `exhibitions` (fuori dalla guardia). Campo `log` per-riga (null oggi) come ponte verso A3.
- **A2. `harness` come asse di comparabilità** ✅ *(fatto, ADR-0011)*: NON un guardato ma un asse *mostrato* — attraversa header/serialize/contratto (default `single`), la tabella aggiunge una colonna `harness` condizionale, la MoA rientra in classifica (#4) come `ensemble` invece che nascosta nella stringa modello. I guardati restano scorer_version/org/k/scaffold/seed: un ensemble entra in classifica solo se li rispetta tutti, altrimenti resta exhibition. La colonna è retro-compatibile (assente se tutte le righe sono `single`; golden byte-identici per leaderboard e scorecard).
- **A3. Righe verificabili contro log committati** ✅ *(meccanismo fatto, ADR-0012)*: il campo `log` diventa `{path, sha256}`; `logs/leaderboard/*.eval` negato in `.gitignore` (come l'holdout, ~130K/log); `--extract` stampa il path repo-relativo + digest; `--verify` (nel job CI) ri-deriva ogni riga log-backed dal suo log e fallisce su digest/metrica non riprodotti (righe `log:null` = unbacked, non fallimento). Confronto solo sui campi log-derivati (mai label/notes), tolleranza di arrotondamento (rate confrontate via `_pct`), path-safety anti-traversal. **Rimane a Rinaldo** committare gli 8 log reali (frontier, non producibili qui) sotto `logs/leaderboard/` per accendere l'enforcement riga per riga (flusso in `logs/leaderboard/README.md`).

**Track B — Chiudere il loop ADR-0008/0009.**
- **B1. scaffold/seed (+factory_version) sul percorso run API/UI**: campi in `RunRequest` (`schemas.py`), task_args in `runner.py`, colonne in `run_store.py` (+ meccanismo minimo di migrazione schema — oggi solo `CREATE TABLE IF NOT EXISTS`), input in `Run.tsx`, display in `Results.tsx` (l'header li porta già).
- **B2. `factory_version` stampata** Score.metadata → RunHeader → riga → `_require_uniform` (il follow-up dichiarato di ADR-0008).
- **B3. La run di holdout dal vivo**: nuovo commitment salato pubblicato PRIMA della run, top model su seed trattenuto, reveal pubblicato — l'unica checkbox aperta del README.
- **B4. Scienza**: refinement R1 (tenere il guadagno di rifiuto senza l'over-refusal di haiku), breadth multi-org oltre la famiglia meridian, follow-up delega (consumer debole/cross-modello/con tool, i non-goal di ADR-0007).

**Track C — Hardening CI & release.**
- **C1. CI**: ruff + mypy (src) + eslint (web) + soglia coverage; il job `contract` resta (documentare la regola di bump dei pin).
- **C2. Release engineering**: tag `v0.1.0` (promesso il 2026-06-23, mai creato), `CHANGELOG.md` (l'URL Changelog di pyproject punta oggi all'indice ADR), workflow di release; PyPI opzionale.
- **C3. Docs-from-manifest**: le copie delle tabelle in README/report generate dal manifest di A1 — elimina le cinque copie a mano che hanno causato l'audit del 2026-07-07.

**Track D — Direzione prodotto (decisioni, non codice).**
- **D1.** Locale-first vs hosted (la decisione «Settimana 4» del §7, ancora aperta).
- **D2.** det-5 (hardening del fallback: i due anchor `xfail` + i 5 fail di format-strictness di qwen) — ripartiziona i trend, stessa scorer_version per riga (ADR-0006).
- **D3.** Topologie di delega oltre il singolo hop.

*(Storia: i «prossimi 3 step» precedenti — write-up ✅ risolto, delegazione follow-up → B4/D3, holdout follow-up → B2/B3.)*

## 7. Domande aperte

- ~~**Ritiro di Streamlit**~~ *(risolto: rimossa — ADR-0004, 2026-06-11)*.
- ~~**Scoring accuratezza**~~ *(det-2, T4)* · ~~**Refusal deterministico**~~ *(det-3)* · ~~**Provenance CRM per-campo**~~ *(det-4, ADR-0005 — il gate live ha anche scoperto e corretto l'ambiguità `{}` sui nomi di campo sbagliati)* · ~~**Dataset pubblico**~~ *(meridian, ADR-0006)*.
- ~~**Leaderboard**~~ *(risolto: `docs/leaderboard.md`, generata da `tessera-leaderboard` — gpt-4o/gpt-4o-mini/haiku-4-5/qwen3.5 + baseline Sonnet, link nel README)*.
- **Distribuzione del prodotto**: FastAPI serve la SPA da `web/dist` — Tessera resta locale-first ("inspector" da lanciare in repo) o diventa un servizio ospitato? (Piano: decisione in Settimana 4.)
- ~~**Reliability under delegation**~~ *(misurato: MVP a 2 stadi — NON via `handoff()`, rigettato con evidenza in ADR-0007 — risultato in `docs/delegation.md`)*. Aperti i follow-up: consumer debole/cross-modello, consumer con tool, catene profonde.
- **det-5 (candidato, hardening del fallback)**: l'aggiudicazione qwen ha quantificato i limiti DOCUMENTATI del percorso senza riga ANSWER — la regola ultima-menzione-vince boccia la risposta trasparente «410 … supersede il 250 stantio» (ordine inverso di quella che passa), e `_REFUSAL_MARKERS` non copre «not available»/«irreconcilable». Contratto pubblicato rispettato (decisione: pubblicare con disclosure, non piegare lo scorer per un modello) — ma 5/12 probe falliti di qwen sono questo: con det-5 la sua riga varrebbe ~68,2% (rank 2). Un eventuale det-5 ripartiziona i trend e RIGIRA tutto il protocollo (stessa scorer_version per riga, ADR-0006).

---

## Diario

- **2026-08-23 (hardening integrità validator)**: tre bug pre-run chiusi prima del freeze del
  panel. Le liste score, prive di identità per posizione, sono diventate mappe task-ID con set
  esatto e ordine JSON irrilevante; ranking denso sostituito dalla posizione competitiva;
  tiebreak lessicale sostituito dall'ordine naturale dichiarato nel prereg. Il contratto
  rifiuta bootstrap/seed assenti, chiavi JSON duplicate, config sia attive sia dropped e
  failure di scrittura output con exit 2. Scanner credenziali spostato dal regex nel test a
  modulo riusabile: niente valori nei finding, pattern sulle forme reali, parametrizzazione su
  tutti i log pubblici. README ripristinato con prerequisito `uv` e comando `judge=llm` con
  grader indipendente; esempio JSON a 7 config eseguito dalla quickstart CI. **395 test**
  (392 passed, 3 xfailed).
- **2026-06-10** — creato STATE.md; audit completo dei docs.
- **2026-06-11 (T1, bonifica)** — working tree (+7383/−1911) spezzato in 7 commit tematici: fix author→run in salvo per primo, poi SPA, lezioni, sync docs (v0 dichiarato, roadmap aggiornata, call-doc rimossi), pulizia root, CI minima. HEAD verde che rappresenta l'app reale. Prossimo: T2 (k parametrico).
- **2026-06-11 (T2, k parametrico)** — il task ora possiede conteggio E reducer: `tessera_probes(k=N)` costruisce `Epochs(k, [pass_k(k), "mean"])`; il runner passa `task_args["k"]` e NON più l'override eval-level (che cambia il conteggio ma tiene il reducer — la causa del bug). `RunRequest.epochs` validato 1..10. Studio verificato su inspect_ai 0.3.235 installato (merge in `_eval/run.py`: epochs e reducer mergiati indipendentemente). Smoke key-free: eval con mockllm a k=2 → log `pass_k_2` → scorecard «pass^2 (strict), 4 × 2 epochs». 163 test. Prossimo: T3 (contratto unico).
- **2026-06-11 (T3, contratto unico)** — ogni endpoint JSON ora dichiara un response model (`api/responses.py`): FastAPI valida ogni risposta, quindi i 166 test key-free sono anche contract test (la rete ha subito beccato una fixture che mentiva sulla forma del report). `web/src/types.ts` non dichiara più nulla a mano: alias dei tipi GENERATI (`api-types.gen.ts`, da openapi-typescript 7.13.0 su `openapi.json` committato; `bash scripts/gen-types.sh`); il drift trovato dall'audit (LogMeta senza `path`) è morto con la generazione. Job CI `contract` fallisce su drift (fastapi/pydantic pinnati lì). Lista modelli unificata: `GET /api/models` alimenta Run.tsx e Streamlit (fallback offline in entrambi); `judge` ora è Literal (typo → 422). Prossimo: T4 (scoring accuratezza).
- **2026-06-11 (push + rebase)** — origin era stato riscritto (purge dalla history dei 3 call-doc, incluso demo-runbook, + un commit README col vero report First Contact): rebase dei 13 commit locali sulla base riscritta, runbook tenuto cancellato per onorare il purge, push fast-forward. Prima run CI su GitHub.
- **2026-06-11 (T4, scoring det-2)** — prima la rete: smoke test sui log pinnati (`tests/test_pinned_examples.py`, numeri headline + testo «$1.5M»). Poi il fix: l'accuratezza deterministica valuta la **risposta committed** — ultima riga `ANSWER:` (il prompt ora la richiede), match con guardie sui confini («24 hours» non colpisce più «4 hours», «115%» ≠ «15%»); senza riga, fallback distractor-aware a ultima-menzione-vince — i distractor derivano meccanicamente dai claim in conflitto del blueprint (`dataset._distractor_values`: solo gruppi (subject,predicate) con valori diversi — «Gold» non diventa mai distractor). `Score.metadata` porta `scorer_version` (det-2/llm-1) e `answer_format_ok`. Scoperta chiave dal recon: **First Contact era motore llm — il substring non c'entrava**; la sua comparabilità non è toccata. 173 test; smoke mockllm end-to-end ok. Prossimo: T5 (ADR).
- **2026-06-11 (T5, refusal det-3 + ADR)** — il contratto det-2 esteso al secondo asse: quando c'è la riga `ANSWER:`, **è lei a decidere anche il rifiuto** («ANSWER: cannot determine» rifiuta; un valore committed sotto ragionamento «coperto» NO — l'astieniti-e-poi-allucina ora viene beccato anche dal motore deterministico, il fallimento First Contact per eccellenza); la scansione keyword resta solo come fallback senza riga. Il recon aveva confermato il buco: nessun test combinava marker di rifiuto + riga ANSWER — ora 2 test lo pinnano. `scorer_version` → det-3. Nato **`docs/adr/`**: 0001 k nel task, 0002 response model = contratto, 0003 risposta committed (con la correzione First Contact a verbale). Docs sincronizzati (README, scorecard guide, lezioni EN/IT — la card «refusal keywords» flippata a shipped). 175 test. Prossimo: push, poi Settimana 3.
- **2026-06-11 (Settimana 3 anticipata)** — (1) **Streamlit ritirata** (ADR-0004): `src/tessera/app/`, `.streamlit/`, `scripts/dev.sh`, entry point e dep rimossi — audit pulito, nulla fuori dal package la importava. (2) **Code-splitting**: React.lazy per vista, recharts confinato nel chunk Dashboard — monolite 794 kB → entry 199 kB, warning sparito. (3) **Prime run det-3 live** (sonnet-4-6, k=3, toy, via API → Dashboard): scoperta chiave — `answer_format_ok` **0/12**, il contratto ANSWER nel prompt non sopravvive al protocollo submit del react agent → tutte le epoch sul fallback, che ha mis-valutato 3/3 risposte resolvable giuste (parentetica in coda + parafrasi date). Fix in 2 passi: prompt riformulato sul testo SUBMITTED (6/12) + contratto nella description del submit tool via `AgentSubmit` (**12/12**, valori esatti). Terza run: **pass^3 75%, mean 92%** — unresolvable 0%/67% flaky: il modello inventa la regola «deal desk più autorevole» 1 volta su 3 e det-3 la becca (`ANSWER: $1.5M` = commitment). Stessa storia di First Contact, ora misurata key-free. «Quote exactly» tolto dal prompt dopo un `ANSWER: 4` senza unità. (4) **ADR-0005 proposto** (provenance per-campo: attribuzione dalla risposta + param fields → det-4; fix anche dell'over-credit su NOT_FOUND). (5) **Design privati**: org pubblica + nota delega in `docs/superpowers/specs/`. 175 test. Prossimo: push, review ADR-0005 e design, poi org pubblica.
- **2026-06-12 (leaderboard + delegation MVP)** — entrambe le voci rimaste di STATE §6, in una sessione. (1) **`tessera-leaderboard`**: modulo puro (`report/leaderboard.py`, l'import inspect_ai resta in cli.py per l'invariante di purezza) + CLI; guardie ESEGUIBILI di comparabilità (scorer_version/org/k misti → ValueError); colonna `ANSWER fmt` strutturale dopo che l'aggiudicazione ha trovato 2 grade-su-wording (contratto documentato, non bug). (2) **Run live**: gpt-4o, gpt-4o-mini, haiku-4-5 (la forma `--model a,b,c` si è IMPIANTATA — socket morti, 0 sample in 51′; rilanciata come 3 processi separati: tutti <2′) + qwen3.5 9.7B locale via Ollama (1h41′, protocollo intero). **Ogni 0/3 aggiudicato da transcript con workflow multi-agente** (38+12 verdetti): gpt-4o salta la gamba CRM dei join cross-silo (ignora il feedback `_available_fields`), mini fallisce join e tutti i tie, haiku ignora il contratto ANSWER (format 3%) e fabbrica su tutti i tie, qwen legge diligentemente (provenance 98%) ma è flaky (mean 71% vs strict 45%). **Tabella: `docs/leaderboard.md`** — `unresolvable` 40/0/0/0/0. Aggiudicazione: 36/38 verdetti API behavioral (2 grade-su-wording da contratto documentato); su qwen 5/12 flag harness della stessa famiglia (fallback su risposte format-noncompliant ma sostanzialmente giuste) → pubblicato con disclosure in tabella + **det-5 candidato** a verbale (§7), contatore controfattuale ~68,2%. (3) **Delegation MVP** (ADR-0007): `handoff()` rigettato con evidenza (content_only distrugge i tool event; senza filtro il consumer vede tutto — le due viste non possono divergere); catena custom di due react via `agent.run()`, transcript fusi, brief nel `store()`, stage limitati a 50 messaggi (mockllm girava a vuoto al 99% CPU — beccato live); scorer det-4 invariato + flag `flag_dropped`/`conflict_laundered`. **Run live Sonnet k=3: 90,9%/95,5%** — delta dal diretto = rumore campionario del producer; il risultato vero è la tabella dei flag: **0/12 refusal corretti distrutti, 3/3 fabbricazioni veicolate** (una razionalizzata esplicitamente dal consumer). `docs/delegation.md` + ADR-0007; README aggiornato (entrambe le checkbox flippate). 210 test. Prossimo: write-up, follow-up delegazione, Settimana 4.
- **2026-06-12 (technical report)** — scritto `docs/report.md` (~4.900 parole, stile arXiv in markdown): il metodo come contributo (ogni sottosezione = una decisione + il fallimento che cattura, ADR citati), related work su 22 benchmark verificati su fonti primarie (workflow di ricerca multi-agente; caveat di venue rispettati), esperimenti = tabelle riprodotte verbatim da `docs/leaderboard.md`/`docs/delegation.md`, ogni numero tracciato sugli artefatti committati. Review: spec-review per sezione (4 fix: ordine delle regole di policy nell'intro, scoping del test di determinismo, attribuzione «38 verdetti su tre run API», dedup del vincolo det-5) + workflow avversario a 5 lenti (numeri/overclaiming/fairness/consistenza/prosa). Prossimo: PR, poi follow-up delegazione / Settimana 4.
- **2026-06-16 (report + ablazione + guardie — 3 PR mergiate)** — chiusura di tre fili in sequenza, ognuno con il suo gate CI. (1) **PR #8, ablazione** (`refactor: ablative simplification`): rimosso solo codice genuinamente morto — 7 componenti shadcn UI mai importati (~391 LOC, `tsc --noEmit` pulito dopo) — più de-dup di `_CANONICAL_ORDER` in un'unica sorgente di verità (`report/models.CANONICAL_ORDER`, importata da render/serialize/leaderboard) e commenti sulle righe portanti (taxonomia ConflictType, validatori Probe/Blueprint, invariante di fusione della delega, guardia path-traversal). `_pct` di render e leaderboard NON unificate di proposito (output diverso). Zero cambio di comportamento provato: 210/210 test, report **byte-identico** fra branch e main, entrambe le CLI identiche, workflow avversario a 3 reviewer. Effetto istruttivo: i docstring degli enum sono finiti nello schema OpenAPI come `description` → il job `contract` ha fallito finché non ho rigenerato `openapi.json` + `web/src/api-types.gen.ts` (la documentazione ora propaga ai tipi del frontend). (2) **PR #7, report tecnico** mergiato (docs-only, zero overlap con l'ablazione → niente conflitti). (3) **PR #9, guardie per-sample** (`perf(evals)`): `message_limit=40` + `time_limit=600` su `tessera_probes` per tagliare un react loop che spirala (comune sui modelli locali lenti) e scartarlo come fallimento invece di appendere la run; sul task delegato **solo** `time_limit=600` — i messaggi sono già limitati PER STAGE (`delegation._STAGE_MESSAGE_LIMIT`), un message_limit a livello Task starebbe sotto e troncerebbe il transcript producer+consumer. `main` pulito, nessun branch/PR aperto. Prossimo: **scenario-factory** (varianti seeded di meridian, mitigazione contaminazione ADR-0006) — sottosistema nuovo, parte da brainstorming.
- **2026-06-23 (scenario-factory + holdout — PR #11, ADR-0008)** — nuovo sottosistema `src/tessera/factory/`. `generate_variant(seed)` deterministico e key-free ri-deala il grafo dei conflitti (quale slot answerable diventa `unresolvable`/`resolvable`, quali campi `void`) e sintetizza valori anti-prior freschi, tenendo fisse le conte di categoria di meridian (6/6/5/5); **seed 0 == `build_meridian_blueprint()` oggetto per oggetto** (test pinnato), così la baseline Sonnet 86,4% resta valida e ri-baselinare il canonico è un non-goal. **Protocollo di holdout**: i numeri di testa di una leaderboard si producono su un seed *trattenuto*; prima della run si pubblica un **commitment salato** `SHA-256(factory_version ‖ 0x00 ‖ seed ‖ 0x00 ‖ salt)` con `salt = os.urandom(32)` (il salt dà l'occultamento — un hash senza salt di un intero piccolo è banalmente forzabile); al reveal si pubblica `{seed, salt, factory_version}` e chiunque ricomputa il digest e rigenera org + chiave di risposta sotto quel `factory_version`. `factory_version` (`fac-1`, bumpata a mano quando schema/pool/lessico/costruttori cambiano gli org generati) è la **nuova dimensione di comparabilità** accanto a scorer_version/org/k; le righe pre-factory valgono `fac-1`/canonical. Property-test sugli invarianti su molti seed (incl. determinismo cross-PYTHONHASHSEED via subprocess) + round-trip/tamper su `commit`/`verify`; CLI `tessera-variant`; tutto key-free e senza rete. **263 test.** Noto, da chiudere: ~0,34% di seed crashano (vedi §3, distractor svuotato da sottostringa). Prossimo: holdout dal vivo, `factory_version` nelle righe di leaderboard, seed su API/UI (i non-goal di ADR-0008).
- **2026-06-23 (review del codice — fix A–C, E)** — risposta alla *Tessera Codebase Review 2026-06-23* (7 reviewer, ogni finding di codice/sicurezza verificato in avversariale; zero RCE/leak/SQLi). Bug di misura: **A1** l'accuratezza sulla riga committed valutava l'intera riga, così `ANSWER: $1.5M (CRM still shows $1.2M)` passava per `$1.2M` — ora match del solo valore committed (testa prima del primo `(` / `, ` / ` not `), inverso del gate già esistente. **A2** `gen_value` garantiva solo la disuguaglianza: un perdente sottostringa del vincitore svuotava i distractor e crashava ~0,34% dei seed (218, 1742, 1771, …), rompendo il reveal di holdout — ora rifiuto delle sotto/sovra-stringhe, regressione su ogni seed 1..1999. **A3** `_committed_refusal` con `startswith` flaggava `ANSWER: Unknown Holdings` come rifiuto — ora il valore committed deve ESSERE un token di astensione. **A4** un template prose malformato (`{nope}`) sfuggiva come 500 — ora un validatore `Claim` fa trial-render col valore reale → 400 strutturato su ogni write path. **A5** l'excerpt di `docs_search` può rivelare il valore mentre la provenance docs si accredita solo via `docs_get_file`: documentato come regola intenzionale (niente redazione — non si cambia uno strumento già pubblicato per ~zero guadagno, provenance già 98-100%). **E**: anchor `xfail(strict)` per det-5 (parafrasi data/importo, negazione nel fallback), `verify()` su salt non-hex torna `False` invece di sollevare, determinismo asserito su 6 seed + guardia `_resolve` (sorgente ignota / stem vuoto / traversal). **C**: versione **0.1.0** (pyproject + `__version__` + CITATION), `requirements.lock` congelato dall'ambiente testato (Python 3.14.5, inspect_ai 0.3.235 — la versione che ha prodotto la leaderboard, **ora registrata anche per-run** su `RunHeader.inspect_ai_version`, contratto rigenerato), matrice CI py 3.10/3.12/3.14, quickstart README con `uv venv`. **270 test** (267 passed, 3 xfailed). La sync di Ch3 della tesi (sezione D della review) è lasciata all'autore. Prossimo: tag `v0.1.0` sul merge, run di holdout dal vivo.
- **2026-06-28 (intervento scaffold refusal-aware — ADR-0009)** — realizzato lo studio H2 della tesi. Parametro `scaffold` su `tessera_probes`: due prompt che differiscono in UN SOLO blocco — `baseline` (B0, il prompt della leaderboard, byte-identico, così le run esistenti restano il braccio B0) vs `refusal_aware` (R1, procedura esplicita detect→classify→escalate sulla tassonomia a 4 tipi); un test fissa il contrasto chirurgico. Contrasto **accoppiato su 5 istanze** (meridian seed 0 + 4 varianti factory), 110 probe-instance per modello per braccio, **McNemar esatto** sui discordanti, decomposto nei due sottoinsiemi di H1₂ (rifiuto unr+void / risposta none+res) così un guadagno pagato in over-refusal resta visibile. Risultato: **amplificatore di capacità, non sostituto** — R1 migliora il rifiuto in modo significativo e senza perdita significativa sul rispondibile per i modelli che già percepiscono il conflitto (Sonnet 4.6 refusal-subset 18/1 p=0.0001, netto 75,5→95,5%; GPT-4o 9/0 p=0.004), mentre haiku paga il guadagno in over-refusal (netto invariato, p=0.86) e gpt-4o-mini non raggiunge mai il pareggio (H0₂ ritenuto). **Holdout** (ADR-0008) eseguito end-to-end: commit `df55a190…` (`fac-1`) PRIMA della run, reveal `{seed=31337, salt, fac-1}` → `verify=True`, rigenerazione deterministica, effetto replicato (unresolvable 40→100%). Docs: `docs/scaffold.md` (tabelle + McNemar + reveal), `scripts/scaffold/{run.sh,analyze.py}`, **ADR-0009**, report sincronizzato (nuovo §6 Experiment 3, rinumerato §1–§8, abstract/limiti/future/riferimenti) e README. **275 test** (272 passed, 3 xfailed). Prossimo: refinement dello scaffold (tenere il guadagno senza over-refusal), follow-up delega, breadth multi-org.
- **2026-07-01 (hardening post-review dello studio scaffold)** — risposta alla code review su PR #13 (9 angoli di ricerca + verifica avversariale per finding; 44 sopravvissuti). (1) **La dimensione `scaffold`/`seed` ora esiste a valle**: `RunHeader` la legge dai task_args (default `baseline`/0 per i log pre-ADR-0009), `report_to_dict` la serializza, `ReportHeader` la espone (contratto rigenerato nello stesso commit), e `tessera-leaderboard` la copre nelle guardie di comparabilità + nel comando repro, con disclosure `⚠️` sulle tabelle non-baseline — una run R1 (+20 punti su Sonnet) non può più entrare silenziosamente in una leaderboard B0. (2) **Statistiche pubblicate riproducibili**: nuovo `report/stats.py` testato (`exact_mcnemar_p`, `format_p`, `mcnemar_counts` che riporta i probe non accoppiati) e `analyze.py` riscritto sopra, ora con la decomposizione H1₂ (subset rifiuto/rispondibile) che i docs headline-ano; verificato contro i log reali — ogni cella e ogni p riprodotti. Due numeri corretti: report §6 attribuiva alla colonna resolvable di haiku i discordanti dell'intero subset rispondibile (14/8 p=0.29 → la colonna è 10/4 p=0.18) e il p refusal-subset di haiku è 0.021, non 0.022; README ora etichetta i p come refusal-subset. (3) **Holdout pubblicato**: `COMMITMENT.json`+`REVEAL.json` tracciati (negazione mirata in `.gitignore`, il resto di `logs/` resta locale), snippet `verify()` nel doc, e caveat onesto: commitment e risultati sono entrati nella history nello stesso commit, il prossimo holdout pubblica il commitment PRIMA della run. (4) **Test-gap chiusi su task.py**: hash golden SHA-256 del prompt B0 (la byte-identity con la leaderboard non era pinnata da nessun test), wiring `_SCAFFOLDS` in entrambe le direzioni, default `scaffold="baseline"` asserito sulla firma, token della tassonomia asseriti sul BLOCCO R1 + nessun `probe_id`/subject meridian nel prompt (niente answer-key leakage), `_SUBMIT_DESC` sincronizzato col contratto condiviso, `from None` sull'errore scaffold ignoto; mutation-check eseguito — entrambe le mutazioni beccate dal test giusto. (5) **Script induriti**: `run.sh` (`set -euo pipefail`, usage, rifiuto senza `.env` PRIMA di ogni `rm -rf`, LOGDIR col provider nel nome, `$INSPECT` overridabile) e `analyze.py` (allowlist `--seeds` 0–4 così una run holdout non inquina mai il pool breadth, skip con warning di log errored/duplicati, guardia di comparabilità k/org/scorer/engine, pooling solo sui seed con entrambi i bracci). Docs repro corretti (`TESSERA_OUT` dedicato + layout `s*/`), CONTRIBUTING richiede `scaffold=baseline` per le righe community. **292 test** (289 passed, 3 xfailed). Rimandato, issue candidate: scaffold/seed su API/UI, tabelle docs generate dai log, refinement R1 senza over-refusal.
- **2026-07-07 (freshness docs + roadmap)** — audit di freshness su tutta la documentazione dopo il merge di PR #15 e la PR #16 (4 righe aggiunte **a mano** a `docs/leaderboard.md`: la guardia di comparabilità non è mai stata eseguita, nessun log committato — righe non rigenerabili). Fix: **MoA spostata in `Out-of-protocol exhibitions`** (un ensemble è un intervento a livello scaffold nascosto nella stringa modello — `scaffold=baseline` per il shim, invisibile a `_require_uniform`: non può stare in classifica fra le righe baseline ADR-0006), tabella principale rinumerata a 8 e nota det-5 di qwen ripristinata; README aggiornato (badge 8_models, «five models» → otto, la claim «every model fabricates tie-breaks» scopata alla coorte 2026-06: **Fable 5 e Opus 4.8 sono le prime righe al 100% su `unresolvable`**, `factory/` aggiunta alla project structure); `docs/report.md` tenuto come snapshot datato del 2026-06-12 con **addendum in §4** (il 40/0/0/0/0 è una proprietà della coorte, non della frontiera); **indice ADR completato** (0008 e 0009 mancavano); header block documentato nella scorecard guide (scaffold/seed inclusi); conteggi test allineati a 292. **Roadmap a 4 track definita in §6**, Track A (integrità leaderboard) per prima. Nessun cambio di codice.
- **2026-07-07 (A1 — leaderboard generata da manifest, ADR-0010)** — chiusa la falla della PR #16 (tabella editabile a mano). `docs/leaderboard.rows.json` è ora la **fonte di verità committata**: `render_manifest` renderizza il markdown, la guardia di comparabilità ADR-0006 gira sulle righe qualunque sia la loro provenienza (log o manifest), le `exhibitions` (MoA) sono fuori-protocollo e bypassano la guardia in una sezione propria. CLI: `--manifest` (render dalla fonte, usato dalla CI) e `--extract` (estrae la riga di un log stampandola col suo sha256 → il campo `log`, null per le 8 righe attuali senza log committato, ponte verso A3). Nuovo job CI `leaderboard`: rigenera il markdown dal manifest e `git diff --exit-code` — una cella scritta a mano, o un manifest che viola la guardia (render esce ≠0), fanno fallire la build. TDD stretto: round-trip manifest≡log, keystone byte-per-byte (`render_manifest(manifest) == docs/leaderboard.md`), exhibitions presenti/assenti + bypass guardia, guardia che scatta via manifest, CLI `--extract`/`--manifest`. Il repro-block del markdown ora descrive il flusso manifest (unica differenza rigenerata vs la tabella scritta a mano; tutto il resto byte-identico). Verifiche negative: hand-edit sovrascritto dal render, guardia scatta su scorer_version misto, `--extract` stampa un digest a 64 char. **297 test** (294 passed, 3 xfailed). ADR-0010 + indice; A2 (dimensione `harness`) rinumerato **candidato ADR-0011**. Prossimo: PR, poi A2/A3 o Track B.
- **2026-07-07 (A2 — `harness` come asse mostrato, ADR-0011)** — la realizzazione chiave (in brainstorm): `harness` è ortogonale ai guardati, quindi NON entra in `_require_uniform` e NON tocca `task.py` (`tessera_probes` È l'harness single-model; un ensemble è un harness diverso, non un suo parametro). Plumbing parallelo a scaffold/seed: `RunHeader.harness='single'`, `log_adapter` legge `args.get('harness','single')`, `serialize`, `ReportHeader` (contratto rigenerato). La leaderboard aggiunge una **colonna `harness` condizionale** (solo se esiste una riga non-`single`, così una tabella tutta-single è byte-identica a prima) + un blockquote di disclosure che nomina il **protocollo identico** (org, k, scorer, scaffold, seed), non un parziale «org/scorer/k». Lo scorecard fa lo stesso con un'annotazione condizionale nell'header. La MoA **migra da `exhibitions` a `rows`** con `harness='ensemble'` + i campi di protocollo, e rientra in classifica al **#4** (81,8%, fra sonnet-4-6 e sonnet-5): l'ensemble sta *sotto* opus-4.8 da solo — i sub-modelli advisory hanno aggiunto rumore, non affidabilità. TDD con le 8 raccomandazioni della review recepite: **golden byte-identici** (file committati) per leaderboard-all-single E scorecard-single (la colonna/annotazione condizionale è dove un test «renderizza ok» mancherebbe una regressione di separatore/allineamento); test «interleaves-by-pass_k» (no segregazione nemmeno nell'ordinamento); harness NON-guardato (tabella mista single+ensemble non solleva); prosa exhibitions aggiornata + pinnata (ora = config con protocollo DIVERSO, non ensemble); disclosure che dice «identical protocol». ADR-0011 marca esplicitamente **superata** l'aspettativa di ADR-0010 (harness come guard) → è una colonna, non una guardia. Valori canonici `single`/`ensemble` documentati in ADR + CONTRIBUTING. **306 test** (303 passed, 3 xfailed). Prossimo: PR, poi A3 (log archiviati + verifica digest) o Track B.
- **2026-07-07 (A3 — righe verificabili contro log committati, ADR-0012)** — consegnata la provenienza che ADR-0010 aveva rimandato («Provenance is A3's job»). Il campo `log` passa da digest nudo a `{path, sha256}` (un digest da solo non è localizzabile → CI non poteva verificarlo); `logs/leaderboard/*.eval` negato in `.gitignore` — attenzione, qui ci sono DUE ignore da battere (`logs/*` E il `*.eval` globale), verificato con `git check-ignore -v`. Split di purezza rispettato: il comparatore è puro in `leaderboard.py` (`row_metric_mismatches`, `LOG_DERIVED_FIELDS`, `_is_safe_repo_relative_path`, `_repo_relative`), l'I/O (lettura log via inspect_ai) resta in `cli.py`. `--extract` normalizza il path a repo-relativo (risolto sul `.git` root, non sul cwd — testato eseguendo da una sottodir con arg relativo) + sha256. `--verify` (con `--manifest`, nel job CI): per ogni riga con `log` non-null rifiuta path non-sicuri (assoluti/drive/backslash/`..`) PRIMA di toccare il filesystem, controlla esistenza + digest, poi **ri-deriva la riga dal log** e verifica i campi log-derivati. Le 5 raccomandazioni della review recepite: (1) gitignore testato con check-ignore; (2) path normalizzato non echo dell'input; (3) `LOG_DERIVED_FIELDS` esplicito e testato al boundary — label/notes editati NON falliscono, una metrica editata SÌ; **tolleranza di arrotondamento** (manifest `0.667` vs log `2/3` → stessa cella `_pct`, non un mismatch); (4) path-traversal rifiutato; (5) exit 2 su fallimento verify (come argparse; CI distingue solo ≠0). Fixture di log realistico (una categoria che passa, una che fallisce, riusato) su repo tmp ermetico (`.git` in tmp_path). Rollout a cricchetto: tutte le righe `log:null` oggi → `--verify` è un no-op che riporta `0/9 backed`; l'enforcement si accende riga per riga appena un log reale viene committato. ADR-0012 + indice; repro-block, CONTRIBUTING, `logs/leaderboard/README.md`. **323 test** (320 passed, 3 xfailed). Rimane a Rinaldo: committare gli 8 log frontier reali. Prossimo: PR, poi Track B/C.
- **2026-07-14 (quality pass — PR #20 core, #22 UI; docs #23/#24)** — pass di qualità sull'intera codebase, standard dichiarato picol/linenoise: ogni riga si guadagna il posto, dipendenze a senso unico, niente mantenuto a mano che una macchina possa derivare. Prima di tagliare, **audit a 3 agenti paralleli** (core Python, web, igiene repo); verdetto onesto: codebase già pulita — finishing pass, non demolizione. **PR #20 (core, comportamento invariato provato)**: alias `reliability_scorer` morto (unico riferimento: un test che asseriva l'esistenza dell'alias — cancellati entrambi); filtro campi CRM **unificato** in `silos/access.py` con i diagnostici `_unknown_fields` (il server MCP diventa un wrapper di 3 righe, wire output byte-identico — `test_mcp_servers` passa intatto); via il seam `key=` di `aggregate_by` (unico caller non-default: un test scritto per esercitare il parametro); nuovo **`tessera/orgs.py`** — risoluzione nome/seed sopra `examples` e `factory`, import a senso unico `examples` ← `factory` ← `orgs`, morto il lazy-import che mascherava il ciclo; **`factory/schema.py` deriva ~70 letterali** (subject/predicate/question) da `build_meridian_blueprint()` a import time — il drift factory↔meridian è impossibile per costruzione, `FACTORY_VERSION` resta `fac-1` (identità seed-0 e property test lo provano); `create_app` (207 righe, 15 handler in una closure) → **4 router per risorsa** via `request.app.state`, contratto **zero-drift byte-per-byte** (gen-types diff vuoto); `_render_doc` (97 righe) → `_table_section`/`_methodology_section`, markdown rigenerato byte-identico; `assert_variant_invariants` → 7 `_check_*` nominati. **PR #22 (UI, l'estetica terminale resta)**: il pattern ovunque è label in parole semplici + termine tecnico declassato a caption, vocabolario single-source in **`web/src/copy.ts`** (i 4 enum di conflitto non possono più divergere fra le viste); **`/learn`** serve la lezione (102 KB, `include_in_schema=False` — contratto intoccato) linkata dalla sidebar; **run-details line** sulla scorecard: scorer_version/seed/scaffold/harness erano calcolati, serializzati nel contratto tipato e MAI renderizzati — ora visibili, con i default soppressi; tile ANSWER-format (parità con la colonna della leaderboard); preview compilata **strutturata** (record account→campo) al posto del `JSON.stringify`; il form Run spiega dataset, indipendenza del grader (prima esisteva solo come errore runtime) e repeats; `api.watchRun` chiude l'unica eccezione al wrapper di rete; 12 stanze label+input → un componente `Field`. **Processo**: subagent-driven con reviewer per task + review finale whole-branch; la review finale ha beccato 2 bug veri sul percorso demo — il banner NOT RELIABLE stampava gli enum grezzi, e `scorer_version` è null su ENTRAMBI i log pinnati → «run details: scorer» penzolante — fixati prima di aprire la PR. **Tenuto di proposito**: `factory/commit.py` (staging ADR-0008), asse `harness` (la riga MoA lo usa), densità di `scoring.py`, estetica terminale. Scoperto sul campo: **main è branch-protected** (push diretto → GH006; tutto via PR + squash). **328 test** (325 passed, 3 xfailed). Limiti: niente click-through browser (verifica = risposte server + grep sul bundle buildato — da qui i 2 bug scampati fino alla review finale); ruff trova 27 errori pre-esistenti nei test (nessun job lint in CI, E402 è la convenzione dei test self-contained); il log-backing della leaderboard resta 0/9 — `--verify` (ADR-0012) è ancora un no-op. Prossimo: committare i log frontier (accende A3), refinement R1, Track B/C.
- **2026-08-23 (v0.2 builder release, ADR-0013)**: il repo era già l'artefatto (132 commit,
  non il README a 3 commit che aveva innescato la review), quindi il lavoro è stato packaging
  e prova. README aperto sul quickstart key-free; tre epoch First Contact portate sopra la
  piega con citazioni verbatim; MCP-AgentBench/Universe/Bench/Mark/Atlas, MCPEval,
  DynamicMCPBench e Toloka aggiunti al related work senza claim di unicità su MCP, generation
  o multirun. Il log First Contact ora ha un receipt a hash: scan credenziali pulito, termini
  provider linkati, contratto semantico uguale al toy org; limite tenuto nel file, la run
  dichiara `dirty=true`, quindi nessuna ricostruzione byte-per-byte della source tree.
  `tessera-validate-transfer` implementa tau-b, bootstrap task-level da 10.000 draw, lower
  bound one-sided, claim language congelato, top-3 e decisive-pair concordance con la regola
  disjoint su entrambe le suite. Preregistration pubblica ma bloccante (`DRAFT` + `PENDING`),
  `fac-1` congelata prima di qualunque pattern dal testbed privato. **380 test** (377 passed, 3 xfailed),
  build SPA ok, contract/leaderboard byte-identici, clean-install quickstart passato. Limiti:
  autorizzazione del data controller, panel/hashes, signed tag prereg e OSF restano gate
  esterni prima delle run.
- **2026-06-11 (batch «completa tutto»)** — su `/goal` esplicito, chiusi tutti i pezzi rimasti. (1) **det-4** (ADR-0005 → Accepted): `consulted_claims` accoppia ogni tool call al suo risultato via `tool_call_id` e accredita un claim CRM solo se il suo predicato è tornato nella RISPOSTA — NOT_FOUND/errori/chiamate senza risposta non accreditano nulla; `crm_lookup` guadagna `fields`; prompt aggiornato (anche: l'autorevolezza dichiarata batte la recency); versioni det-4/llm-2. (2) **Scorecard**: `scorer_version` nell'header, `answer_format_ok` per epoch, `answer_format_rate` negli assi — tutto nullable, contratto rigenerato nello stesso commit. (3) **Meridian**: 10 account, 47 claims, 22 probe (6/6/5/5), authority che inverte la recency, valori anti-prior; 7 gate offline nel suite. (4) **Review avversaria** (3 audit paralleli): zero blocker; fix — piano «Larkspur» (il vecchio nome echeggiava la sua risposta), 110 minuti fuori dalla griglia SLA, prompt genericizzato, rifiuto ancorato sulla riga committed («ANSWER: $425k (no record…)» è un impegno), estrazione ANSWER tollerante al markdown. (5) **Gate live**: la PRIMA baseline ha beccato un difetto del harness — l'agente indovinava il nome campo (`fields=["seats"]`), riceveva `{}` ambiguo e concludeva che il CRM era vuoto; ora `crm_lookup` risponde `_unknown_fields`+`_available_fields` (lo scorer ignora le chiavi di feedback). Baseline rifatta: **pass^3 86,4% / mean 90,9%** (Sonnet 4.6, k=3, det-4) — none/resolvable/void 100%, provenance 1.0, format 65/66, **unica categoria che fallisce: unresolvable 2/5 flaky** (fabbrica precedenze sul pareggio). Cross-check llm-2 (gpt-4o): 72,7%/84,9%, stesso profilo. **ADR-0006**: meridian = benchmark, blueprint pubblico (onestà su purezza), leaderboard det+k=3 con scorer_version pubblicato. Docs sincronizzati (README, scorecard guide, lezioni EN/IT — card provenance e «4 probe» flippate a shipped). 189 test. Prossimo: leaderboard multi-modello, delegation MVP, Settimana 4 (hosting).
